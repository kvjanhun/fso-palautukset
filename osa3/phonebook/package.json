{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Backend for phonebook app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../osa2/phonebook && npm run build && cp -r dist ../../osa3/phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "KJ",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.16.5",
    "morgan": "^1.10.1"
  }
}
